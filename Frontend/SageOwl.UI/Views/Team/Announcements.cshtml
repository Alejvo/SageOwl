@{
    ViewBag.Title = "Team Announcements";
    Layout = "~/Views/Shared/Layouts/_WorkspaceLayout.cshtml";
}

@model TeamAnnouncementsPageViewModel

<main class="team_announcements_main">
    @await Component.InvokeAsync("TeamNavigation", new {teamId = Model.TeamId})
    <div class="team_announcements_main_container">
        @if (Model.Announcements.Count == 0)
        {
            <div class="team_announcements_img">
                <img src="~/images/team_work.svg" alt="Team_Work" width="400" height="400"/>
                <h5>There are no <b>Announcements</b> yet</h5>
            </div>
        }
        else
        {
            @foreach (var announcement in Model.Announcements)
            {
                @await Html.PartialAsync("~/Views/Shared/PartialViews/_Announcement.cshtml", announcement)
            }
        }

    </div>
</main>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).on("click", ".nav-icon", function () {
            const targetUrl = $(this).data("url");
            if (targetUrl) {
                window.location.href = targetUrl;
            }
        });
    </script>
}